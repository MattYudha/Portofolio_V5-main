import React from 'react';

// This component no longer renders its own Canvas
// The 3D navigation is now handled by Main3DScene
const Navbar3D = () => {
  return null; // The 3D navigation is now rendered in Main3DScene
};

export default Navbar3D;